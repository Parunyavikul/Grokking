/* eslint-disable no-unused-vars */
/**
 * Given a string, find the length of th elongest substring in it with no more than K distinct characters.
 */

function longest_substring_no_more_than_k_distinct_chars(str, k){
  let maxLength =0,
      start = 0,
      charFreq = {};


  return maxLength;
}
